/*
  Supabase config — replace the placeholders with your project details.
  Do NOT commit real keys to public repos. Use environment variables or CI secrets for production.
  Example usage (in browser):
    <script src="/supabase/config.js"></script>
    <script type="module">import { supabase } from '/supabase/client.js';</script>
*/
window.SUPABASE_CONFIG = {
  url: 'https://your-project.supabase.co',
  anonKey: 'your-anon-key'
};
